import { bootstrap } from '@lirx/dom';
import { AppLazyLoadingExampleComponent } from './component/lazy-loading-example.component';

export function lazyLoadingExample() {
  bootstrap(AppLazyLoadingExampleComponent);
}
