import { bootstrap } from '@lirx/dom';
import { AppBindEventExampleComponent } from './component/bind-event-example.component';

export function bindEventExample() {
  bootstrap(AppBindEventExampleComponent);
}
